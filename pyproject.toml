[project]
name = "revise-langraph"
version = "0.1.0"
description = "Hands-on scenarios for learning LangGraph"
requires-python = ">=3.14"
dependencies = [
    "langchain>=1.3.18",
    "langchain-anthropic>=1.7.0",
    "langgraph>=1.2.11",
    "python-dotenv>=1.2.3",
]

[dependency-groups]
dev = [
    "debugpy>=1.8.21",
]
